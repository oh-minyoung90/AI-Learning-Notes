# 루프 엔지니어링 (Loop Engineering)

> **한 줄 정의**
> 사람이 매 단계 지시하는 대신, "목표 → 실행 → 검증 → 수정 → 종료"의 사이클을 AI 에이전트가 **스스로 반복하도록 반복 구조 자체를 설계**하는 것.
> 핵심은 반복문(`while`)이 아니라, **언제 멈출지(종료 조건)** 와 **무엇이 나아졌는지(누적 학습)** 를 기계가 판단하게 만드는 데 있다.

---

## 0. 왜 지금 루프 엔지니어링인가

한 문장으로 요약하면, **"에이전트를 프롬프트하지 말고, 에이전트를 굴리는 루프를 설계하라"** 는 흐름이다.

Claude Code를 만든 개발자 Boris Cherny는 한 인터뷰에서 "이제 나는 AI에 프롬프트를 쓰지 않는다. 내가 하는 일은 루프를 짜는 것"이라는 취지의 발언을 했다. 주방장이 "이제 주문은 안 받는다"고 말하는 격의 도발적 표현이지만, 뜻은 분명하다 — **작업의 단위가 '프롬프트 한 줄'에서 '반복 시스템 전체'로 옮겨 갔다**는 것이다.

이것이 한 사람의 유행어가 아니라는 근거는, 업계가 거의 동시에 같은 방향을 가리켰다는 데 있다.

- Peter Steinberger(@steipete): "코딩 에이전트를 프롬프트하지 말고, 에이전트를 프롬프트하는 **루프를 설계하라**" (널리 공유됨)
- Google의 Addy Osmani: 같은 주제를 장문의 아티클로 정리하며 루프를 조립하는 **부품 6가지**를 제시(본문 5장)

> ⚠️ 확인 필요: 위 인용문·수치(좋아요 수 등)는 세미나 발표 자료에 근거한 것으로, 원문 표현과 출처는 실제 인터뷰/아티클/트윗으로 교차 확인이 필요하다. 개념의 골자는 신뢰할 만하나, **정확한 워딩을 인용할 때는 1차 출처를 확인**할 것.

---

## 1. 작업 단위의 진화 — 프롬프트에서 루프까지

'○○ 엔지니어링'이라는 이름은 지난 2년간 네 번 바뀌었다. 이름이 바뀐 게 아니라, **레버리지(지렛대)를 거는 지점이 네 번 이동**한 것이다. 각 단계는 언제나 **직전 단계의 한계**에서 태어났다.

| 단계 | 무엇을 설계하나 | 어떤 한계에서 나왔나 |
|---|---|---|
| ① 프롬프트 엔지니어링 | 코드를 직접 치는 대신 "이렇게 만들어 줘"라고 잘 요청하는 법. 작업 단위 = 타이핑 → 프롬프트 | (출발점) |
| ② 컨텍스트 엔지니어링 | 모델이 한 번에 기억할 수 있는 분량(컨텍스트 윈도우)을 **무엇으로 채울지** 설계 | 프롬프트를 잘 써도 부딪히는 **기억의 한계** |
| ③ 하네스 엔지니어링 | 자율적으로 움직이는 에이전트가 엉뚱한 곳으로 튀지 않게 **안전장치(harness)** 를 다는 법. 일하는 **'공간'** 의 설계 | 에이전트 **자율성의 위험** |
| ④ 루프 엔지니어링 | 위의 모든 것을 사람이 매번 시키지 않고 **에이전트가 스스로 돌도록** 반복 구조를 설계. 도는 **'시간'** 의 설계 | 사람이 매 바퀴 지시하는 **비효율(=병목)** |

**하네스가 일하는 '공간'을 설계한다면, 루프는 반복이 도는 '시간'을 설계한다.** 그래서 루프는 하네스 위에서 돈다.

### 레버리지가 옮겨 갔다

| | 예전의 레버리지 | 지금의 레버리지 |
|---|---|---|
| 잘해야 하는 것 | 프롬프트 한 줄을 잘 쓰는 것 | 에이전트를 굴리는 **시스템을 통째로 설계**하는 것 |

일이 사라진 게 아니다. **레버리지 지점이 프롬프트 작성 → 시스템 설계와 지속적 감시로 이동**했을 뿐이다. (이 문장은 6장 마지막에서 다시 회수된다.)

### 오늘 쓸 최소 용어 4개

| 용어 | 쉬운 뜻 | 기억할 포인트 |
|---|---|---|
| **LLM** | 글을 읽고 쓰는 대규모 AI 모델 | ChatGPT·Claude의 두뇌. '모델'이라 하면 전부 이것 |
| **토큰(Token)** | 모델이 글을 세는 단위 = **요금의 단위** | **토큰을 태운다 = 돈을 쓴다** |
| **컨텍스트 윈도우** | 모델이 한 번에 기억할 수 있는 분량 | 창을 넘어가면 잊는다. **실행이 끝나도 전부 잊는다** |
| **에이전트(Agent)** | 도구를 쓰며 여러 단계를 스스로 수행하는 AI | 한 번 답하고 끝이 아니라, 파일을 고치고 터미널을 돌리고 결과를 보고 다음 행동을 정한다 |

강조 2개 — **① 토큰 = 돈**(뒤에서 '토큰 먹는 하마'가 반복 등장), **② 모델은 실행이 끝나면 전부 잊는다**(뒤의 '메모리' 이야기의 복선).

---

## 2. 루프란 무엇인가 — 다섯 단계로 보면 단순하다

루프의 골격은 다섯 단계다.

```
목표 → 실행 → 검증 → (실패 시) 수정 → (통과 시) 종료
                ↑___________________|
```

이 사이클 자체는 AI 이전부터 있던 소프트웨어 개발의 기본형이다. 테스트를 돌리고(검증), 실패하면 고치고(수정), 다 통과하면 배포하는(종료) 흐름 — 사실상 **테스트 주도 개발(TDD)과 CI(지속적 통합)의 사고방식**을 에이전트에게 넘긴 것이다. 학술적으로는 "추론하고(Reason) 행동한다(Act)"를 번갈아 반복하는 **ReAct 계열 에이전트 패턴**의 연장선에 있다.

### 예전에는 사람이 바퀴를 손으로 굴렸다

- "이거 만들어 줘" → 결과 보고 → "검증해 줘" → 틀리면 → "이 부분 고쳐 줘"
- **매 바퀴마다 사람이 프롬프트를 직접 쳤다.**
- 검토·테스트가 사람 손에 있는 한, **사람이 루프의 병목(bottleneck)** 이 된다. AI의 생산 속도엔 한계가 없는데, 그 결과를 사람이 하나하나 확인해야 하면 전체 속도는 사람에 묶인다.

### 루프를 설계한다 = 이 사이클이 사람 없이 돌게 만든다

- 사람은 한 발 물러나 **목표와 종료 조건만** 정의한다.
- 바퀴를 굴리는 일(실행·검증·수정의 반복)은 에이전트에게 넘긴다.
- 즉, **병목인 사람을 루프 밖으로 빼낸다.**

한 줄 정의로 다시: **"모든 테스트가 통과할 때까지 실행 → 검증 → 수정을 에이전트가 스스로 반복하는 것."**

---

## 3. 진짜 루프를 가르는 두 질문

> **"그냥 `while` 문 아니에요?"** — 맞다. 루프 자체는 단순하다.

`while` 문은 누구나 쓸 수 있다. 어려운 것은 **'언제 멈출지'와 '무엇이 좋아졌는지'를 기계가 판단하게 만드는 일**이다. 그래서 다음 두 질문에 "예"라고 답할 수 있어야 비로소 단순 반복이 '엔지니어링'이 된다.

### 질문 ① — AI가 결과를 신뢰할 만하게 '평가'할 수 있나?

루프의 3단계는 '검증'이다. 그런데 **검증하는 주체가 못 미덥다면?** 루프가 수십 바퀴를 돌아도 그것은 개선이 아니라 **환상**이다.

> 🎯 비유: **잘못된 채점관**을 두고 시험을 100번 본들, 그 점수에는 아무 의미가 없다.

시험을 100번 보게 하는 건 쉽다. 채점이 엉터리면 100번의 응시가 전부 무의미하다. → 이 질문의 답이 **조건 1(독립된 검증자)**.

### 질문 ② — 한 바퀴 돌 때마다 에이전트에게 '남는 것'이 있나?

루프를 수십 번 돌며 일했는데 끝나고 나서 **아무 학습도 남지 않는다면?** 그것은 자기 개선이 아니라 그냥 '반복 자동화'일 뿐이다.

> 🎯 비유: 매번 똑같은 실수를 **처음처럼 다시** 저지른다면, 바퀴를 굴리는 의미가 없다.

이 질문의 답이 **조건 2(누적되는 메모리)**.

### 두 질문에 실패하면 — '토큰 먹는 하마'

> 명확한 종료 조건과 검증 기준 없이 돌리는 루프는 자기 개선이 아니라 **'토큰 먹는 하마'** 다.

여기서 역설이 하나 있다. **비싸고 강력한 모델일수록 루프를 더 잘 설계해야 한다.** 종료 조건 없이 풀어 놓으면, 똑똑하게 일하는 게 아니라 **똑똑하게 돈을 태우기** 때문이다. (토큰 = 돈)

---

## 4. 진짜 루프의 두 조건

두 질문에 "예"라고 답하려면, 루프에 두 가지 장치가 필요하다. 1:1로 대응한다.

| 조건 | 무엇을 하나 | 어느 질문의 답인가 |
|---|---|---|
| **조건 1 · 독립된 검증자** | 작성자와 채점관을 분리해 '완료'를 판정 | 질문 ①(신뢰할 평가) |
| **조건 2 · 누적되는 메모리** | 바퀴마다 배운 것을 디스크에 남겨 다음 바퀴가 이어받음 | 질문 ②(남는 학습) |

**검증자가 '언제 멈출지'를 판정하고, 메모리가 '무엇이 나아졌는지'를 쌓는다.** 이 둘이 갖춰져야 단순 반복이 '개선'이 된다.

### 조건 1 — 독립된 검증자 (신뢰할 수 있는 평가)

**원칙: 자기 평가(self-critique)보다, 독립된 검증자 서브 에이전트(verifier sub-agent)의 채점이 더 믿을 만하다.**

| 방식 | 어떻게 | 문제/장점 |
|---|---|---|
| 자기 평가 | 내가 짠 코드를 내가 채점 | 사람처럼 모델도 **자기 출력에는 관대**하다 |
| 검증자 서브 에이전트 | 별도 에이전트가 **독립된 컨텍스트**에서 채점 | 남의 결과물은 깐깐하게 본다 |

> 🎯 비유: **시험 답안지를 옆 반 선생님이 채점하게 하는 것.** 내 코드는 내가 봐도 잘 짠 것 같고, 남의 코드는 이상한 곳부터 보인다 — 모델도 똑같다.

왜 자기 평가가 위험한가(배경 지식): LLM은 자신이 생성한 출력을 다시 볼 때 이미 그 방향으로 편향돼 있고, 사용자·자기 답에 동조하려는 경향(sycophancy)이 있다. 그래서 **생성자(generator)와 비평자(critic)를 물리적으로 분리**하는 evaluator–optimizer 패턴이 더 견고하다.

실무 구현 포인트:

- Claude Code의 `/goal`, Claude Managed Agent의 Outcomes 등은 **독립 컨텍스트에서 채점하는 별도 채점관(grader)** 을 자동으로 띄워, 작성자와 채점관을 물리적으로 분리한다.
- 채점관에게는 **체크 가능한 기준을 모은 루브릭(채점 기준표)** 을 쥐여 준다. 예: "모든 테스트 통과", "grep 결과 0건", "문서 갱신됨".
- **전 기준 충족을 확인하기 전에는 종료 금지.**

### 조건 2 — 누적되는 메모리 (남는 학습)

**복선 회수: 모델은 실행이 끝나면 모든 것을 잊는다.** 루프의 바퀴 하나하나가 바로 그 '실행'이다. 바퀴가 끝나면 그 안에서 겪은 시행착오도 함께 사라진다.

그렇다면 '어제의 실패에서 배우는 루프'는 어떻게 가능한가?

- 바퀴(세션)와 바퀴 사이를 잇는 다리는 오직 하나 — **공유 메모리(디스크 위의 파일)** 뿐이다.
- 기억하고 싶다면, **기억은 디스크에 있어야 한다.** (마크다운 파일, 보드 등)
- 결론: **루프의 성장은 '적는 것을 얼마나 잘하는가'에 달렸다.**

#### 메모리 사다리 — 좋은 메모리는 다섯 단계를 오른다

| 단계 | 이름 | 하는 일 | 오답 노트 비유 |
|---|---|---|---|
| ① | 실패 기록 | 안 됐던 것을 일단 적는다 | 틀린 문제를 노트에 적기 |
| ② | 조사 | 왜 안 됐는지 파고든다 | 왜 틀렸는지 찾기 |
| ③ | 검증 | 추측을 실제로 확인한다 | 진짜 그런지 확인 |
| ④ | 정립 | 확인된 것을 **일반 규칙**으로 만든다 | 공식으로 정리 |
| ⑤ | 참조 | 다음 작업에서 그 규칙을 꺼내 쓴다 | 다음 시험에서 써먹기 |

**낮은 단계에서 멈추면 메모리는 '실패 노트 더미'일 뿐이다.** ⑤까지 올라가야 루프가 돌 때마다 남는 것이 생긴다. 대부분의 사람(그리고 모델)이 ①~②에서 멈춘다. **검증된 일반 규칙(④) 하나가 미검증 추측 노트 열 개보다 낫다.**

---

## 5. 루프는 부품으로 조립한다 — Addy Osmani의 6부품

1~4장이 '무엇이 진짜 루프인가'(개념·두 조건)를 정의했다면, 이 장은 '실무에서 무엇으로 조립하는가'(부품)를 다룬다. 다섯 개의 부품 + 하나의 척추(Memory) 구조다. **대부분은 하네스 엔지니어링에서 이미 쓰던 것** — 루프 엔지니어링은 새 부품의 발명이 아니라, **그 부품들이 사람 없이 계속 돌게 조립하는 것**이다.

| 부품 | 역할 | 비유 |
|---|---|---|
| **Automations** | 스케줄에 맞춰 자동 발동 — 스스로 할 일을 발견·분류 | 루프의 **심장** |
| **Worktrees** | 여러 에이전트 병렬 작업 시 파일 충돌 방지 | 작업자마다 **독립된 책상** |
| **Skills** | 프로젝트 지식을 재사용 가능하게 저장(SKILL.md) | 매번 안 가르쳐도 되는 **사내 매뉴얼** |
| **Plugins · Connectors** | 이슈 트래커·DB·Slack 등 외부 도구와 연결 | 세상과 잇는 **손과 발** |
| **Sub-agents** | 코드 작성과 검증을 분리한 여러 에이전트 | 작성자와 **별도 검수자** |
| **+ Memory** | 마크다운·보드 등 외부 저장소에 상태 기록 | 망각을 보완하는 **디스크 위의 기억(척추)** |

> Memory에 `+`가 붙은 이유: 다섯 부품과 결이 다른, 전체를 지탱하는 **척추**이기 때문이다.

### 부품 깊이 보기

**Automations — 루프의 심장**
- 지정한 주기마다 프롬프트/명령을 실행하고, 발견 사항을 Triage(분류) 인박스로 보낸다.
- 실무 예: 일일 이슈 분류, CI 실패 요약 등.
- Claude Code라면 `/goal`로 "auth 디렉터리의 모든 테스트 통과" 같은 **검증 가능한 종료 조건**을 건다.
- 포인트: 사람이 시작 버튼을 누르는 한 진짜 자동이 아니다. **심장은 스스로 뛴다.**

**Worktrees — 작업자마다 독립된 책상**
- Git worktree로 같은 저장소의 여러 브랜치를 **독립 체크아웃**으로 분리한다. (하나의 `.git` 저장소를 공유하되, 각 브랜치가 별도 작업 디렉터리를 갖는 Git 기능)
- 두 에이전트가 같은 파일을 건드려도 충돌하지 않고, 작업이 끝나면 자동 정리된다.
- 루프가 항상 옳게 도는 건 아니므로, **메인 코드를 망치지 않게 격리하는 안전판** 역할도 한다.

**Skills — 사내 매뉴얼**
- 프로젝트 지식을 `SKILL.md` 같은 파일로 재사용 가능하게 저장한다.
- 원칙: **"영리한 표현보다 간결하고 지루한 설명이 더 효과적"** — 의도의 빈틈을 에이전트의 '자신만만한 추측'으로 메우게 두면 안 된다. 멋있게 쓰지 말고 **오해 불가능하게** 써라.

**Connectors — 손과 발 (MCP)**
- 외부 도구와 표준 방식으로 연결하는 계층. MCP(Model Context Protocol)는 에이전트가 이슈 트래커·DB·메신저 등 외부 시스템과 대화하기 위한 표준 인터페이스라고 이해하면 된다.
- 실제로 PR을 열고 → 티켓 연결 → CI 확인 → Slack 알림까지 이어진다.
- **도구가 없으면 루프는 중간에 멈춰 사람을 부른다** — '멈추지 않는 루프'를 위해 필요한 커넥터를 미리 채워 둔다.

**Sub-agents — 별도 검수자**
- 작성자는 자기 결과에 관대하므로(4장 조건 1) 독립 검증자를 둔다.
- 모델 등급 배분: 보안 리뷰는 고성능 모델, 단순 탐색은 빠른 모델. 토큰은 더 들지만 신뢰성이 오른다.

**Memory — 장기 루프의 척추**
- 모델은 실행 사이에 전부 잊으므로 상태는 디스크(마크다운·보드)에 남긴다.
- 회전마다 상태를 기록해 **멈춰도 이어서 재개**할 수 있다.
- 4장의 '메모리 사다리'가 이 부품의 작동 원리다.

### 부품 조립 예시 — 아침마다 스스로 도는 루프

```
[매일 아침]
Automation이 깨어난다
   → Worktree에 격리된 책상을 편다
      → Sub-agent가 고치고 검증한다
         → Connector가 PR·티켓·알림을 처리한다
            → Memory에 기록을 남긴다
[다음 날 아침] 그 기록에서 다시 시작한다
```

내가 잠든 사이에 에이전트가 어제의 기록에서 이어서 CI 실패를 분류하고, 고치고, 검증하고, PR을 열고, 기록을 남겨 둔다.

---

## 6. 루프의 한계 — 그래도 사람의 몫은 남는다

한계를 정직하게 다루는 것이 이 기술을 제대로 쓰는 길이다. **무인 루프는 무인으로 실수하는 루프이기도 하다.**

### 루프가 풀어 주지 못하는 세 가지

| # | 함정 | 뜻 |
|---|---|---|
| ① | **검증 부담** | 검증 에이전트가 "완료"라고 말해도 그것은 **증명이 아니라 주장**이다. 평가가 옳은지 최종 책임지는 건 사람 |
| ② | **이해 부채 (Comprehension Debt)** | 내가 직접 쓰지 않은 코드가 빠르게 쌓일수록 '존재하는 코드'와 '내가 이해하는 코드'의 간극이 벌어진다. **빨리 출시할수록 빚이 쌓인다** |
| ③ | **인지적 항복 (Cognitive Surrender)** | 루프가 알아서 돌면 사람은 스스로 판단하기를 멈추고 결과를 그냥 수용하기 쉽다 — **가장 위험한 함정** |

그래서 결론은 하나로 모인다 — **"엔지니어로 남아라 (Stay an engineer)."**
신입에게는 특히 ②③이 치명적이다. **이해 없이 쌓는 속도는 성장이 아니라 부채다.**

### 같은 루프, 정반대 결과

> "한 명은 깊이 이해한 일을 더 빨리 하려고 루프를 쓰고, 다른 한 명은 일을 이해하지 않으려고 루프를 쓴다."

| 올바르게 쓰면 | 잘못 쓰면 |
|---|---|
| 깊은 이해 위에서 반복 작업만 자동화 → **속도 향상** | 버튼만 누르며 책임 회피 → **품질 악화와 하향 나선** |

같은 도구가 쓰는 사람에 따라 성장 가속기도, 성장 정지 버튼도 된다. **일이 사라진 게 아니라 레버리지가 이동했다**(1장 회수). 루프가 짜 준 코드를 읽고 이해하는 습관을 들이면 도구가 성장을 가속하고, 결과만 받아 넘기면 몇 년 뒤 설명할 수 있는 코드가 남지 않는다.

### 현실적인 토큰 비용

루프는 강력하지만 **토큰을 먹는 괴물**이기도 하다. "작업 두 번에 월 정액 한도를 다 태웠다"는 사례가 흔하다.

**비용 최적화 패턴 — 비싼 모델에게는 '리드 엔지니어' 역할만 맡겨라.**

| 작업 성격 | 맡길 모델 |
|---|---|
| 간단한 팬아웃(fan-out) 작업 | 상대적으로 저렴한 경량 모델 |
| 복잡한 구조 설계 · 검증 · 장기 루프 | 고성능 모델 (리드 엔지니어) |

> 🎯 비유: 시니어(비싼 모델)에게 단순 반복 업무를 맡기면 낭비다. 시니어는 설계·검증·어려운 문제에, 반복 작업은 주니어(저렴한 모델)에게.

이렇게 역할을 나누면 비용을 **약 1/3 수준**까지 줄일 수 있다(세미나 제시 수치 — 실제 절감폭은 작업 구성에 따라 달라짐). **"언제 비싼 모델을 부르고 언제 싼 모델로 충분한가"를 설계하는 일 자체가 새로운 엔지니어링 과제**다.

---

## 7. 실전 가이드 — 첫 루프를 돌려 보자

### 첫 루프 체크리스트 (5가지)

- [ ] **① 작은 진짜 과제 하나를 고른다.** 예: 실패하는 테스트 고치기, 린트 오류 0개 만들기. **반복해서 하는 귀찮은 일이 최고의 첫 과제.**
- [ ] **② 종료 조건을 '기계가 확인 가능'하게 쓴다.** 감상평("괜찮으면 끝") 말고 — pytest 통과, grep 결과 0건, 수치 기준(응답 시간 200ms 이하)처럼.
- [ ] **③ '하지 말 것'(제약)도 반드시 명시한다.** 모델은 기준의 헐거움을 정확히 파고든다. 쓰면 안 되는 라이브러리, 건드리면 안 되는 파일을 명시적으로 막아라.
- [ ] **④ 작성자와 채점관을 분리한다.** `/goal`처럼 독립 검증자에게 채점을 맡긴다 — 자기 평가는 후하다(4장 조건 1).
- [ ] **⑤ 끝나면 코드를 직접 열어 읽는다.** "완료"는 주장이지 증명이 아니다 — 이해 부채와 인지적 항복을 막는 유일한 습관.

### 종료 조건 — 이렇게 쓰면 하마, 이렇게 쓰면 루프

| ✗ 하마를 부르는 종료 조건 | ✓ 루프를 만드는 종료 조건 |
|---|---|
| "3D 게임을 잘 만들어 줘" | pytest 전부 통과 — 실패 0건 |
| "코드 품질을 좋게 해 줘" | 린트 경고 0건 (린터 명령어로 검사) |
| "다 되면 알려 줘" | 응답 시간 200ms 이하 (측정 스크립트로 검사) |
| → '잘'·'좋게'·'다'를 기계가 판정 못 함 | 제약 명시: "외부 라이브러리 추가 금지" |
| → 제약이 없어 어떤 편법도 허용됨 | → 전부 명령어 한 줄로 참/거짓 판정 가능 |

> **기억할 것 — 좋은 기준도 뚫린다.** 모델은 기준의 빈틈을 정확히 파고든다(명세 게이밍/reward hacking). 좋은 종료 조건은 한 번에 완성되지 않는다. **뚫리면 기준을 보강하며 반복하는 것, 그것이 루프 '엔지니어링'이다.**

### 언제 프롬프트로, 언제 루프로? (판별 4문)

넷 다 "예"면 루프 과제다.

1. **성공을 기계가 판정할 수 있는가?** (pytest·grep·수치 기준) — 판정 불가면 루프는 언제 멈출지 모르는 하마가 된다.
2. **본질적으로 시도 → 확인 → 수정의 반복이 필요한가?** — 한 번의 응답으로 끝나는 일(설명·이름 짓기·간단한 유틸)은 프롬프트가 더 싸고 빠르다.
3. **반복마다 사람이 기다리는 것이 병목인가?** — 사람의 판단 자체가 핵심인 일(설계 결정 등)은 루프에 맡기지 않는다.
4. **실패해도 격리·복구가 쉬운가?** — Worktree로 격리 가능한 작업. 되돌리기 어려운 일은 루프에 맡기면 위험하다.

> **한 번 할 일은 손(프롬프트)으로, 반복될 일은 루프로.** 스크립트 자동화를 언제 만들지 판단하는 것과 똑같다.
> 넷 다 "예"인 예시: 테스트 전부 통과 · 린트 0건 · 마이그레이션 · 매일 아침 분류.

### 루프의 이점은 두 층위

| 층위 | 내용 |
|---|---|
| **1차 이점 · 자율 반복** | 실행 → 검증 → 수정이 사람 없이 목표까지 돈다 — **병목 제거**. 한 세션 안에서 직전 시도의 피드백을 보고 고친다. (루프의 기본값) |
| **2차 이점 · 기록의 복리** | 같은 작업을 다시 돌릴 때, 이전 실행이 남긴 기록에서 시작해 더 잘한다 — **조건 2(누적 메모리)가 만드는 복리**. 설계했을 때만 생긴다 |

**단, 발전은 자동이 아니다.** 모델은 실행이 끝나면 전부 잊으므로, 발전의 실체는 모델이 아니라 **디스크의 기록**(메모리 사다리 완주)이고, "발전했다"는 판정은 **독립 검증자 위에서만** 성립한다. 루프가 가장 빛나는 자리 = **기계가 판정 가능한 종료 조건 + 주기적으로 반복되는 작업**(두 이점이 동시에 걸린다).

### 오늘 밤 실행 레시피

한 줄로 요약된 실행 명령(예시):

```
/goal "auth 테스트 전부 통과 — pytest 실패 0건 · 외부 라이브러리 추가 금지"
```

이 한 줄에 **목표 · 종료 조건 · 독립 채점관 · 자율 반복**이 동시에 성립한다.

| 단계 | 할 일 |
|---|---|
| ① 과제 고르기 | 반복되는 귀찮은 일 하나 — 실패하는 테스트, 린트 경고 0건 만들기 |
| ② 제약 적기 | `CLAUDE.md`에 '하지 말 것' 두 줄 — "외부 라이브러리 추가 금지", "이 디렉터리 수정 금지" |
| ③ 루프 걸기 | 위 `/goal` 한 줄 — 채점관이 모든 기준 충족을 확인하기 전에는 멈추지 않는다 |
| ④ 기록 붙이기 (선택) | "시도와 교훈을 `PROGRESS.md`에 남겨라" 한 문장 추가 — 다음 루프가 이어받는다(조건 2를 손으로 구현) |
| ⑤ 직접 읽기 | "완료" 보고가 오면 diff를 직접 연다 — 기준 통과가 곧 의도 달성은 아니다 |

---

## 8. 핵심 요약 — 네 문장

1. **루프 = 목표 → 실행 → 검증 → 수정 → 종료의 자율 반복.** 단, 신뢰할 평가와 남는 학습(메모리)이 없으면 '토큰 먹는 하마'다.
2. **진짜 루프에는 두 조건이 필요하다.** 독립된 검증자(질문 ①의 답)와 누적되는 메모리(질문 ②의 답) — 검증자가 종료를 판정하고, 메모리가 개선을 쌓는다.
3. **루프는 부품으로 조립된다.** Automations · Worktrees · Skills · Connectors · Sub-agents + Memory.
4. **그래도 사람의 몫은 남는다.** 목표·제약 설계, 검증 책임, 이해의 유지 — 루프는 엔지니어를 대체하는 게 아니라 **레버리지를 옮긴다.**

> 외울 것은 단 하나 — **두 가지 질문**("평가를 믿을 수 있나? · 바퀴마다 남는 게 있나?"). 나머지는 전부 이 두 질문의 응용이다.
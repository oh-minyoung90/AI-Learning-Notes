# 키워드 검색 (Keyword Search / Lexical Search)

> 문서에 **그 단어가 실제로 들어 있는가**를 기준으로 찾는 고전적 검색 방식.
> 한 줄 요약: **"의미가 아니라 '단어 일치'로 찾는다."**

> 관련 노트: 임베딩/벡터 검색(Sparse 검색의 확장) · RAG · 컨텍스트 엔지니어링.
> 임베딩 노트가 "의미로 찾기(Dense)"라면, 이 문서는 그와 짝을 이루는 "단어로 찾기(Sparse)"를 깊게 본다.

---

## 1. 키워드 검색이란?

사용자가 입력한 **단어(키워드)가 문서에 등장하는지**를 보고 찾는 방식이다.
구글 초창기 검색, 사내 문서 검색, 로그 검색 등 우리가 오래 써온 대부분의 검색이 여기 해당한다.

- **의미 검색(벡터)** 과 대비된다: 의미 검색은 "환불"로 "반품 규정"을 찾지만, 키워드 검색은 **"환불"이라는 글자가 있어야** 찾는다.
- 단순해 보이지만, **정확한 단어·고유명사·코드·숫자**를 찾는 데는 여전히 가장 강력하다.

> 다른 이름으로 **Lexical Search(어휘 검색)**, **Sparse Retrieval(희소 검색)** 이라고도 한다.

---

## 2. 핵심 원리

### 2.1 역색인 (Inverted Index)

키워드 검색의 심장. **"단어 → 그 단어가 있는 문서 목록"** 형태로 미리 뒤집어 저장해둔 것이다.

```
문서1: "환불 규정 안내"
문서2: "배송 규정 변경"

역색인:
  "환불" → [문서1]
  "규정" → [문서1, 문서2]
  "배송" → [문서2]
```

- 검색할 때 문서를 처음부터 다 읽는 게 아니라, **역색인에서 단어를 바로 찾아** 해당 문서 목록을 꺼낸다. 그래서 빠르다.
- 비유: **책 뒤의 "찾아보기(색인)"** 와 같다. 단어를 보고 몇 페이지에 있는지 바로 안다.

### 2.2 토큰화 (Tokenization)

문서와 질의를 **검색 단위(토큰)로 쪼개는** 과정. 역색인을 만들려면 먼저 텍스트를 단어로 나눠야 한다.

- 영어는 대체로 공백으로 나누면 되지만, **한국어는 훨씬 까다롭다** (5장 참고).
- 보통 소문자화, 불용어(the, 은/는 등) 제거, 어근 정규화(stemming/lemmatization)도 함께 한다.

### 2.3 TF-IDF — 단어의 중요도

어떤 문서가 질의와 얼마나 관련 있는지 점수를 매기는 고전적 방법.

| 요소 | 뜻 | 직관 |
|------|-----|------|
| **TF (단어 빈도)** | 그 단어가 문서에 자주 나올수록 ↑ | 많이 언급 = 관련 높음 |
| **IDF (역문서 빈도)** | 그 단어가 **드문** 단어일수록 ↑ | 흔한 단어(예: "그리고")는 변별력 낮음 |

> 직관: **"이 문서에 자주 나오면서(TF), 다른 문서엔 잘 안 나오는(IDF) 단어"** 가 그 문서의 핵심이다.

### 2.4 BM25 — 실무 표준

TF-IDF를 개선한 랭킹 함수로, **오늘날 키워드 검색의 사실상 표준**이다.

#### 어떻게 검색하나 (동작 순서)

BM25는 **"질의에 대해 각 문서의 관련 점수를 매기고, 높은 순으로 줄 세우는"** 일을 한다.

1. **질의를 토큰화**: "환불 규정" → `환불`, `규정`
2. **역색인으로 후보 문서만 추림**: `환불`·`규정`이 든 문서만 (전체를 안 뒤짐)
3. **각 문서에 점수 부여**: 질의 단어별 점수를 계산해 **합산**
4. **점수 순 정렬**해 반환

#### 점수를 매기는 3원리

문서 점수 = 질의에 든 각 단어의 점수를 더한 것. 단어 하나의 점수는 아래 셋이 곱해진다.

| 원리 | 뜻 | 직관 |
|------|-----|------|
| **① IDF (귀함)** | 드문 단어일수록 ↑ | "그리고"는 변별력 0, "환불"은 가치 큼 |
| **② TF + 포화** | 자주 나올수록 ↑, 단 상한 존재 | 2번→20번이라고 10배 중요하진 않음 |
| **③ 문서 길이 보정** | 평균보다 긴 문서는 감점 | 짧은데 그 단어가 있으면 "주제다" 가점 |

```
문서 점수 = Σ (질의의 각 단어)  IDF × [ TF에 포화·길이보정 적용 ]

조절 손잡이:  k1(≈1.2~2.0)=TF 포화 속도,  b(≈0.75)=길이 보정 강도
```

#### 예시 — 질의 "환불 규정" (환불=드문 단어, 규정=흔한 단어 가정)

| 문서 | 내용 | 결과 | 이유 |
|------|------|------|------|
| **A** | "환불 규정 안내. 환불은 7일 이내" (짧음) | **최고점** | 두 단어 다 있고, 귀한 "환불" 포함, 짧아서 가점 |
| **B** | 아주 긴 안내문, "환불" 5회 | 중간 | ②포화로 5회 이득 제한 + ③길이 감점 |
| **C** | "배송 규정 변경" | 최저 | 흔한 "규정"만, "환불" 없음 |

→ 순위 **A > B > C**. **"귀한 단어가, 적당히 자주, 짧은 문서에 있을 때"** 높은 점수. 이게 BM25의 핵심 감각이다.

#### 왜 TF-IDF 대신 BM25인가

- 순수 TF-IDF는 단어 빈도에 따라 점수가 **계속 선형 증가**하고 길이 보정이 약하다.
- BM25는 여기에 **포화(②)** 와 **길이 보정(③)** 을 더해, 긴 문서나 단어 도배에 휘둘리지 않게 개선했다. 그래서 실무 표준.
- 대부분의 검색 엔진(Elasticsearch/OpenSearch 등)이 기본 랭킹으로 BM25를 쓴다.

> 세부 수식은 몰라도 된다. **"귀한 단어를, 적당히 자주, 짧은 문서에서"가 고득점** 이라는 감각이면 충분.

---

## 3. 질의 방식

키워드 검색은 정밀한 조건 검색이 강점이다.

| 방식 | 예 | 설명 |
|------|-----|------|
| **단순 매칭** | `환불 규정` | 해당 단어가 든 문서 |
| **불리언(Boolean)** | `환불 AND NOT 배송` | AND/OR/NOT 조합 |
| **구문(Phrase)** | `"환불 규정"` | 정확히 그 순서로 붙은 경우만 |
| **와일드카드** | `환불*` | 접두/접미 패턴 |
| **퍼지(Fuzzy)** | `환불~` | 오타 허용(편집 거리) |
| **필드 지정** | `title:환불` | 특정 필드 안에서만 |

> 의미 검색으로는 어려운 "정확히 이 문구", "이 필드에서만", "이 단어 제외" 같은 조건을 **정밀하게** 걸 수 있다.

---

## 4. 장점과 한계

### 장점
- **정확한 단어·고유명사·코드·숫자·ID**에 강하다 (예: 에러코드 `E4012`, 제품명).
- 결과의 이유가 **명확하다**(그 단어가 있어서 나온 것) → 설명·디버깅 쉬움.
- 임베딩 불필요 → **인덱싱이 가볍고 빠르며 저렴**하다.
- 정밀한 조건 검색(불리언·필드·구문) 가능.

### 한계
- **동의어·표현 차이를 못 잡는다**: "환불" ↔ "반품", "notebook" ↔ "laptop".
- **의미·문맥을 모른다**: 단어만 보고 뜻을 이해하지 못함.
- **다국어·오타에 약함**: 형태 변화가 크면 놓친다.

---

## 5. 한국어 키워드 검색의 특수성 (중요)

한국어는 영어와 달라 **토큰화가 검색 품질을 좌우**한다.

- **조사 문제**: "환불", "환불을", "환불은"이 공백 기준으론 다 다른 단어가 된다 → 검색 누락.
- **교착어 특성**: 어간 + 어미/조사가 붙어 한 어절이 됨 ("먹었습니다" = 먹 + 었 + 습니다).
- **띄어쓰기 불규칙**: "인공지능" vs "인공 지능".
- **복합어**: "검색엔진" ↔ "검색 엔진".

→ 그래서 한국어는 **형태소 분석기(morphological analyzer)** 로 의미 단위를 뽑아 색인해야 한다.

- 형태소 분석기가 "환불을" → `환불` + `을(조사)` 로 나눠주면, "환불"로 검색해도 걸린다.
- 대표적으로 검색 엔진의 한국어 분석기(예: Nori), 형태소 분석기(예: Mecab, Okt 계열)를 쓴다.

> 요령: 한국어 키워드 검색이 "당연히 있어야 할 문서를 못 찾는다"면, 십중팔구 **형태소 분석/토큰화 설정 문제**다.

---

## 6. 언제 쓰나 — 그리고 Hybrid

| 상황 | 키워드 검색이 유리? |
|------|---------------------|
| 정확한 고유명사·제품명·코드·ID | ✅ 매우 유리 |
| 법률·규정처럼 정확한 용어가 중요 | ✅ 유리 |
| 동의어·의역·개념 검색 | ❌ 의미 검색(벡터)이 유리 |
| 오타·표현 다양성 | ❌ 의미 검색 보완 필요 |

- 실무 정답은 대개 **Hybrid(키워드 + 의미)** 다. 키워드로 정확성을, 벡터로 의미 확장을 잡는다.
- 결합 방법(RRF 등)은 임베딩/벡터 검색 노트 3.2 참고.

> 기억할 점: **의미 검색이 등장했다고 키워드 검색이 낡은 게 아니다.** 정확 매칭 영역에서는 여전히 최강이며, 둘을 합칠 때 가장 강력하다.

---

## 7. 대표 도구

> **먼저 오해 풀기: 도구마다 검색 로직이 다른 게 아니다.**
> 핵심 원리(역색인·BM25)는 대부분 **같다**. 실제로 Elasticsearch·OpenSearch는 모두 **Apache Lucene** 기반이라 알맹이가 사실상 동일하다.
> 차이는 검색 방식이 아니라 **"그 엔진을 얼마나 크게·편하게·다른 기능과 함께 포장했는가"** 에 있다.
> 비유: **BM25가 엔진**이라면, 도구들은 그 엔진을 담은 서로 다른 탈것이다.

| 도구 | 정체 | 차이나는 지점 |
|------|------|----------------|
| **Apache Lucene** | 검색 **라이브러리**(엔진 부품) | 알맹이 그 자체. 서버·분산·API는 직접 붙여야 함 |
| **Elasticsearch** | Lucene을 감싼 검색 **서버/플랫폼** | 분산·확장, REST API, 집계·분석, 대시보드, 벡터·하이브리드까지. 본격 검색용 |
| **OpenSearch** | Elasticsearch에서 갈라진 오픈소스 | 기능 유사, 라이선스·거버넌스가 다름 |
| **PostgreSQL 전문검색** | DB 내장 검색 | 이미 쓰는 DB 안에서 가볍게. 대규모·정교함은 전용 엔진보다 약함 |
| **BM25 라이브러리** (rank_bm25 등) | 코드에 넣는 경량 함수 | 서버 없이 소규모·프로토타입용. 운영 기능 없음 |

### 무엇을 고를까 — 기준은 "규모와 운영 부담"

| 상황 | 추천 |
|------|------|
| 이미 PostgreSQL을 쓰고 검색은 부수 기능 | PostgreSQL 전문검색 (도입 부담 최소) |
| 소규모·프로토타입, 서버 세우기 부담 | BM25 라이브러리 |
| 대규모 문서, 본격 검색·분석·확장 | Elasticsearch / OpenSearch |
| 한국어 형태소 분석·하이브리드 검색까지 | Elasticsearch / OpenSearch (Nori·벡터 지원) |

> 정리: **검색 로직이 다른 게 아니라, 그 로직을 담은 규모·포장·부가기능이 다르다.**
> "검색이 서비스의 핵심이고 문서가 많다"면 전용 엔진, "곁다리거나 작다"면 DB 내장·라이브러리로 가볍게.

---

## 8. 다른 노트와의 관계

- **임베딩/벡터 검색**: 키워드 검색 = 그 노트의 **Sparse Retrieval**. Dense(의미)와 대비·결합된다.
- **RAG**: RAG의 검색 단계를 벡터 대신(또는 함께) 키워드로 구성할 수 있다.
- **GraphRAG / 온톨로지**: 앞으로 다룰 그래프 기반 검색과 대비되는 "텍스트 매칭" 접근.

---

## 9. 참고자료

- BM25 / TF-IDF 원리 자료
- Elasticsearch·OpenSearch 공식 문서 (분석기·쿼리)
- 한국어 형태소 분석기(Nori, Mecab, Okt 등) 문서
- 관련 노트: 임베딩/벡터 검색, RAG

---

# GraphRAG (Graph-based RAG)

> 문서를 벡터로만 보지 않고, **지식 그래프(개체·관계)로 만들어 그 연결을 따라가며** 검색하는 RAG의 확장.
> 한 줄 요약: **"검색 경로에 지식 그래프를 쓰는 RAG."**

> 관련 노트: RAG(벡터 기반 표준 RAG) · 온톨로지(지식 그래프의 밑바탕) · 키워드 검색 · 임베딩/벡터 검색.
> RAG 노트가 "청크를 벡터로 검색"한다면, 이 문서는 "개체·관계 그래프를 탐색"하는 방식을 다룬다.

---

## 1. GraphRAG란?

일반 RAG는 문서를 **조각(청크)** 으로 나눠 벡터로 저장하고, 질문과 **의미가 비슷한 조각**을 찾는다.
GraphRAG는 여기에 **지식 그래프**를 더한다. 문서에서 **개체(엔티티)와 그들의 관계**를 뽑아 그래프로 만들고,
질문에 등장하는 개체를 그래프에서 찾은 뒤 **연결된 관계를 따라가며** 필요한 정보를 모은다.

- Microsoft Research가 **2024년** 제안하며 널리 알려졌다.
- 핵심 차이는 **데이터 표현**이다: 일반 RAG = 흩어진 청크(벡터), GraphRAG = 연결된 개체·관계(그래프).

> 정의(널리 인용되는 표현): **"GraphRAG는 검색 경로에서 지식 그래프를 쓰는 RAG다."**
> 그래프만 쓴다는 뜻이 아니라, 검색 과정에 그래프를 활용한다는 의미.

---

## 2. 일반 RAG의 한계 → GraphRAG가 푸는 것

일반 RAG의 약점은 **정보를 "서로 끊긴 조각"으로 본다**는 점이다. 그래서 아래 질문에 약하다.

- **다단계 추론(multi-hop)**: "A가 만든 것에 참여한 사람이 또 무엇과 관련 있나?" — 여러 관계를 연달아 타야 하는 질문.
- **문서 간 종합(cross-document)**: 답이 한 조각이 아니라 여러 문서에 흩어져 있고, 그걸 **연결해야** 나오는 경우.
- **전체 조망(global sensemaking)**: "이 자료 전체의 주요 주제는?"처럼 데이터셋 전반을 아우르는 질문.

GraphRAG는 개체를 **연결로 표현**하므로, 이런 "연결을 따라가는" 질문에서 강점을 보인다.

### 왜 그래프가 유리한가 — 예시

일반 RAG(청크 검색)로는 어려운 질문:
> "봉준호가 감독한 영화에 출연한 배우가, 다른 어떤 감독과도 작업했나?"

이건 **감독→영화→배우→다른 영화→다른 감독** 으로 관계를 여러 번 타야 한다.

```
[봉준호] ─감독─▶ [기생충] ◀─출연─ [송강호] ─출연─▶ [다른영화] ◀─감독─ [다른감독]
```

그래프에서는 이 경로를 **따라가기만** 하면 되지만, 청크 벡터 검색은 각 조각이 끊겨 있어 이 연결을 재구성하기 어렵다.

---

## 3. 동작 방식

일반 RAG처럼 **인덱싱(사전 준비)** 과 **질의(검색+생성)** 로 나뉜다. 인덱싱이 특히 무겁다.

### 3.1 인덱싱 (그래프 구축)

1. **청킹**: 문서를 조각으로 나눈다. (여기까지는 일반 RAG와 같음)
2. **개체·관계 추출**: LLM으로 각 조각에서 엔티티와 그들의 관계를 뽑는다. (예: (봉준호, 감독, 기생충))
3. **그래프 구축**: 뽑은 트리플을 모아 지식 그래프(노드=개체, 엣지=관계)를 만든다.
4. **커뮤니티 탐지·요약**: 서로 밀접히 연결된 개체 무리(커뮤니티)를 찾고, 각 무리에 대한 **요약**을 미리 생성해둔다. → 전체 조망 질문에 쓰임.

### 3.2 질의 — Local vs Global

| 방식 | 언제 | 어떻게 |
|------|------|--------|
| **Local search (지역 검색)** | 특정 개체에 대한 구체적 질문 | 질문 속 개체를 그래프에서 찾아, **주변 이웃·관계**를 탐색 |
| **Global search (전역 검색)** | 데이터 전체를 아우르는 질문("주요 주제는?") | 미리 만든 **커뮤니티 요약**을 종합해 답 |

- 질문의 개체를 그래프에서 식별 → 관계를 따라 관련 정보를 모음 → 그 컨텍스트로 LLM이 답 생성.

---

## 4. 일반 RAG vs GraphRAG

| | 일반 RAG (벡터) | GraphRAG (그래프) |
|---|---|---|
| 데이터 표현 | 흩어진 청크의 벡터 | 개체·관계의 그래프 |
| 검색 방식 | 의미 유사도 top-K | 개체 식별 + 관계 탐색 |
| 강점 | 단일 조각으로 답하는 질문, 세부 사실 | 다단계·문서 간 종합·전체 조망 |
| 약점 | 연결·종합이 필요한 질문 | 구축 비용·시간 큼 |
| 근거 추적 | 청크 출처 | 관계 경로(설명 가능성↑) |
| 구축 비용 | 낮음(며칠) | 높음(수 주~수 개월, 온톨로지·추출) |

> 벤치마크상 **어느 하나가 항상 이기지 않는다.** 단일·세부 질문은 일반 RAG, 다단계·전역 질문은 GraphRAG가 유리. 그래서 **둘을 결합**하는 하이브리드가 흔히 최선이다.

---

## 5. 언제 쓰나

| 상황 | 추천 |
|------|------|
| 답이 한 문단에 있는 단순·세부 질문 | 일반 RAG |
| 여러 관계를 타야 하는 다단계 질문 | GraphRAG |
| 여러 문서를 연결·종합해야 함 | GraphRAG |
| "전체 주제/경향"을 묻는 전역 질문 | GraphRAG (커뮤니티 요약) |
| 근거·경로 설명이 중요(의료·법률·금융) | GraphRAG |
| 빠르고 저렴하게 시작 | 일반 RAG로 시작, 필요 시 그래프 추가 |

> 실무 원칙: **일반 RAG로 시작 → "연결·종합·다단계"에서 한계를 느끼면 GraphRAG(또는 하이브리드)로 확장.**

---

## 6. 비용·한계 (중요)

- **인덱싱 비용이 크다**: 개체·관계 추출과 커뮤니티 요약에 LLM을 많이 호출한다. 초기 Microsoft GraphRAG는 대규모 데이터에서 인덱싱 비용이 매우 높은 것이 문제로 지적됐다.
- **구축·유지 부담**: 좋은 그래프를 만들려면 온톨로지 설계·추출 품질 관리가 필요. 지식이 바뀌면 갱신도 필요.
- **추출 품질 의존**: 개체·관계를 잘못 뽑으면 그래프 전체가 부정확해진다 (다국어·모호성 주의).
- **경량 변형들**: 비용·복잡도를 낮춘 후속 방법이 많이 나왔다 (예: LightRAG, HippoRAG 등). 커뮤니티 요약을 단순화하거나 부분 그래프만 탐색해 비용을 줄인다.

> ⚠️ 빠르게 발전 중인 분야다. 특정 구현·비용은 최신 자료로 확인할 것.

---

## 7. 온톨로지 · 지식 그래프 · GraphRAG의 차이

세 개념은 "같은 재료를 다른 단계에서 부르는 이름"이라 헷갈리기 쉽다. 집짓기 비유로 정리한다.

> **온톨로지 = 설계도, 지식 그래프 = 그 설계도대로 지은 건물, GraphRAG = 그 건물을 활용해 답을 찾는 서비스.**

| 단계 | 무엇인가 | 비유 | 실제 데이터? | AI 관련? |
|------|----------|------|--------------|----------|
| **온톨로지** | 개념·관계의 종류와 규칙(틀) | 설계도 | ❌ (틀만) | ❌ |
| **지식 그래프** | 그 틀에 채운 실제 데이터 | 지어진 건물 | ✅ | ❌ |
| **GraphRAG** | 그 그래프로 검색해 LLM이 답 | 건물을 쓰는 서비스 | ✅ 활용 | ✅ |

단계별로:

1. **온톨로지(설계도)**: "감독·영화·배우가 있고, 감독은 영화를 감독한다"는 **규칙만**. 아직 "봉준호"는 없다.
2. **지식 그래프(건물)**: 그 틀에 실제 데이터를 채움 → `(봉준호)-감독-(기생충)-출연-(송강호)`.
3. **GraphRAG(서비스)**: 그 그래프를 창고로 삼아 답한다.
   ```
   질문: "봉준호 영화에 나온 배우는?"
     → 그래프에서 [봉준호] 찾기
     → 관계 따라가기: 봉준호 ─감독─▶ 기생충 ◀─출연─ 송강호
     → 이 정보를 LLM에 근거로 넘김 → "송강호입니다"
   ```

> 셋이 꼭 다 필요하진 않다. **온톨로지 없이** LLM이 문서에서 개체·관계를 자유 추출해 지식 그래프를 만들 수도 있다(구축 쉬움·일관성↓).
> 잘 정의된 온톨로지가 있으면 그래프 품질↑ → GraphRAG 답변 정확도↑. 자세한 트리플·관계 개념은 온톨로지 노트 참고.

---

## 8. 다른 노트와의 관계

- **RAG**: GraphRAG는 RAG의 검색 경로를 그래프로 바꾼(또는 더한) 확장.
- **온톨로지**: 지식 그래프의 구조적 밑바탕.
- **키워드 검색 / 임베딩**: GraphRAG와 함께 하이브리드로 결합되곤 한다 (그래프 + 벡터 + 키워드).
- **컨텍스트 엔지니어링**: 그래프에서 모은 관계·요약이 컨텍스트 창에 들어간다.

---

## 9. 참고자료

- Microsoft Research, GraphRAG (Edge et al., 2024)
- 경량 변형: LightRAG, HippoRAG 등 후속 연구
- Neo4j 등 그래프 DB 및 지식 그래프 자료
- 관련 노트: RAG, 온톨로지, 키워드 검색, 임베딩/벡터 검색

---

*본 문서는 개인 학습 정리 노트입니다. GraphRAG는 빠르게 발전 중이므로 최신 구현·비용은 별도 확인 권장.*
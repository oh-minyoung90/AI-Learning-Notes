# 개발 용어 정리

조사 과정에서 반복 등장하는 개념을 정리한 용어집이다. 각 용어는 "정의 → 왜 필요한가 → 비유/보충"의 순서로 서술한다. 새로운 용어는 해당 범주에 이어서 추가한다.

---

## 1. 실행 엔진 · 코드 실행

### Code Interpreter (코드 인터프리터)

LLM이 생성한 코드를 실제로 실행해주는 별도 서비스이자 도구다. LLM 자체는 텍스트만 생성할 뿐 코드를 실행하지 못하므로, "AI가 계산했다 / 그래프를 그렸다"가 성립하려면 코드를 대신 실행해주는 엔진이 필요하다. 실행 흐름을 만드는 주체가 아니라, 실행 흐름이 호출하는 하나의 도구(잎사귀)라는 점이 중요하다.

### Sandbox (샌드박스)

코드가 실제로 실행되는, 외부와 차단된 격리 환경이다. AI가 생성한 코드는 신뢰할 수 없으므로(버그·악의적 코드 가능성) 실제 서버에서 바로 실행하면 위험하다. 따라서 격리된 방 안에서만 실행하고, 대개 실행이 끝나면 폐기한다. 실행 결과는 stdout(정상 출력) / stderr(에러 출력)로 반환된다.

### Docker / 컨테이너

애플리케이션과 그 실행에 필요한 모든 것을 하나의 격리된 상자(컨테이너)에 담아 실행하는 도구다. "내 PC에선 되는데 서버에선 안 된다" 문제를 해결한다. 가상머신(VM)이 OS까지 통째로 새로 띄우는 무거운 방식이라면, 컨테이너는 호스트 OS 커널을 공유하고 프로세스만 격리하는 가벼운 방식이다. 샌드박스를 만드는 가장 흔한 기술이 Docker 컨테이너다.

---

## 2. 멀티 에이전트 협업

여러 방식의 차이는 "다음에 무엇을 실행할지 누가 결정하느냐"에 있다. 대체로 Chain → Handoff → Orchestrator 순으로 점점 더 유연하고 중앙집중적인 구조가 된다.

| 방식 | 순서 결정 주체 | 성격 |
|------|--------------|------|
| Agent Chain | 등록 시점에 사람이 고정 | 정적 워크플로우 (A→B→C) |
| Handoff | 현재 일하는 Agent의 LLM | 수평·동적 라우팅 (제어권 이동) |
| Orchestrator | 전담 지휘자(Supervisor) | 수직·동적 오케스트레이션 (배정·취합) |
| Subagent | 부모 LLM이 위임 | 독립 Child 실행 |

### Agent Chain (에이전트 체인)

여러 전문 Agent를 미리 정한 순서대로 실행하는 방식이다. 순서가 등록·편집 시점에 고정되며, 다음 Agent 선택을 위한 판단이 실행 중에 일어나지 않는다. 초안 작성 → 검토 → 편집처럼 고정 순서가 중요한 작업에 적합하다. 정해진 결재 라인에 비유할 수 있다.

### Handoff (핸드오프)

실행 도중 현재 Agent가 "이건 다른 Agent가 더 낫겠다"고 판단해 제어권을 넘기는 것이다. 넘길지 여부와 대상을 LLM이 실행 중에 스스로 선택한다는 점이 핵심이다. 넘길 수 있는 후보는 미리 등록되어 있고, 실행 시 각 후보로 이동하는 Transfer Tool이 현재 Agent에게 주어진다. 상담원이 "기술팀이 낫겠네요" 하고 전화를 넘기는 것에 비유할 수 있다. 넘긴 뒤 자신은 빠진다.

### Orchestrator (오케스트레이터 / Supervisor)

여러 Agent 위에 있는 전담 지휘자가 사용자의 목표를 해석하고 적절한 Agent에게 업무를 배정한 뒤, 그 결과를 받아 다음 행동을 다시 결정하는 방식이다. 일꾼 Agent들은 서로에게 제어권을 넘기는 것이 아니라 항상 지휘자에게 결과를 보고한다. 지휘자가 중앙에서 판단하므로 여러 Agent에게 동시에 일을 시키는 병렬 실행, 반복 검토, 토론 형태의 협업을 구성하기 쉽다. PM이 팀원들에게 업무를 분배하고 결과를 취합하는 것에 비유할 수 있다.

**Handoff와의 차이:** 둘 다 실행 중 LLM이 동적으로 결정한다는 점은 같지만, 결정 주체와 구조가 다르다. Handoff는 결정권이 현재 일하는 Agent에게 있고(수평·분산, 넘기면 자신은 빠짐), Orchestrator는 결정권이 전담 지휘자에게 있다(수직·중앙, 제어권이 항상 지휘자에게 되돌아옴). 그래서 Handoff는 한 곳에서 다음 한 곳으로 넘어가는 단선적 흐름에, Orchestrator는 병렬·토론 같은 다대다 협업에 적합하다.

### Subagent (서브에이전트)

부모 Agent가 특정 작업만 자식 Agent에게 위임하고 그 결과를 돌려받아 자신이 계속 진행하는 방식이다. 자식은 독립된 실행 흐름(별도 Context)으로 동작하지만, 여기서 "독립"은 LLM Context와 도구 실행 흐름의 분리를 의미하며 OS 프로세스나 컨테이너 분리를 의미하지는 않는다. 뒤에 있는 전문가에게 물어보고 답만 받아 자신이 계속 응대하는 것에 비유할 수 있다.

---

## 3. 웹 서버 · 요청 처리

### Express 서버

Node.js에서 웹 서버를 만드는 대표적인 프레임워크다. 들어오는 HTTP 요청을 받아 적절히 처리하고 응답을 돌려주는 서버를 쉽게 구현하게 해준다. 핵심 기능은 라우팅(어떤 URL을 어떤 함수가 처리할지 연결)과 미들웨어(요청 전처리 체인)다.

### 미들웨어 체인 (Middleware Chain)

요청이 최종 처리 로직에 도달하기 전에 순서대로 거쳐 가는 중간 처리 단계들의 나열이다. 각 단계는 요청을 통과시키거나(next), 거부하거나(401/403/429 등 응답), 가공한다(다음 단계용 정보 부착). 공항 보안검색대에 비유할 수 있다. 관심사 분리를 위해 인증·레이트리밋·권한 등을 각각 독립 미들웨어로 쪼갠다.

**순서의 중요성:** 예를 들어 PII 필터는 외부 모더레이션 API 호출보다 앞에 있어야 한다. 민감정보를 먼저 걸러내지 않으면 외부로 유출될 수 있기 때문이다. 미들웨어 순서 하나가 보안 사고로 이어질 수 있다.

### JWT (JSON Web Token)

사용자가 로그인된 상태임을 증명하는 디지털 신분증이다. HTTP는 요청마다 서로를 모르는 무상태(stateless) 구조이므로, 로그인 시 발급받은 토큰을 이후 요청마다 제시해 신원을 증명한다(`Authorization: Bearer {token}`). 헤더·페이로드·서명 세 부분으로 구성되며, 서버만 아는 비밀키로 만든 서명 덕분에 위조를 막는다.

**주의:** 페이로드는 암호화가 아니라 인코딩이므로 누구나 내용을 열어볼 수 있다(위조만 불가). 비밀번호 등 민감정보를 넣으면 안 된다. 보통 미들웨어 체인의 맨 앞에서 검증되며, 실패 시 401로 즉시 차단된다.

---

## 4. 응답 전달 · 스트리밍

### stream 방식 vs invoke 방식

LLM 실행 결과를 받아오는 두 가지 방식이다.

| 구분 | invoke (한 번에) | stream (조금씩) |
|------|-----------------|----------------|
| 결과 반환 | 완성된 뒤 통째로 | 생기는 대로 조각 단위로 |
| 사용자 체감 | 완료까지 빈 화면 대기 | 타이핑되듯 실시간 표시 |
| 중간 개입 | 어려움 | 가능 |

최종 결과물은 두 방식이 동일하며, stream은 정확도가 아니라 "받는 타이밍"이 다를 뿐이다. 스트림 조각은 화면 표시용 근사치이므로, 최종적으로 서버가 확정한 정본으로 교체하는 처리가 필요하다.

### SSE (Server-Sent Events)

서버가 하나의 HTTP 연결을 열어둔 채 데이터를 조각조각 계속 써 내려보내는 단방향 스트리밍 방식이다. 답변 토큰을 실시간으로 화면에 흘려보내기 위해 사용한다. 브라우저 기본 `EventSource`는 GET만 지원하므로, 질문 본문을 POST body로 보내야 하는 경우에는 POST 위에 SSE를 얹는 별도 라이브러리를 쓰기도 한다.

---

## 5. 그래프 기반 실행 구조

### 실행 그래프 (Graph) / 노드 / 엣지

에이전트 실행 흐름을 그래프 형태로 표현한 구조다. 공장의 컨베이어 벨트 배치도로 이해할 수 있다.

- **노드(node):** 작업 스테이션 — 모델 호출 노드, 도구 실행 노드, 에이전트 노드
- **엣지(edge):** 다음 스테이션으로 가는 레일 — 조건 분기, Agent 간 handoff
- **state:** 벨트에 실려 흐르는 상자 — 메시지 배열, 세션 정보 등
- **superstep:** 벨트가 한 칸 전진 — 노드 1회 실행 사이클
- **checkpointer:** 정지 중 상태를 넣어두는 금고 — 그래프 상태를 DB에 저장

요청마다 그래프를 새로 조립하는 방식이라도, 코드를 생성해 실행(codegen)하는 것이 아니라 설정 객체를 채워 엔진에 넘기면 엔진이 컴파일하는 방식일 수 있다(레고 조립에 가깝다). 이 경우 `eval` / `new Function` 같은 동적 코드 실행은 사용하지 않는다.

### ToolNode

그래프 안에서 도구를 실제로 실행하는 노드다. 모델 노드가 "이 도구 써줘"라고 tool call을 내면, ToolNode가 실행하고 결과를 state에 넣어 다시 모델 노드로 돌려준다.

### 생성 작업 (Generation Job)

한 번의 답변 생성을 서버 측 객체(Job)로 관리하는 단위다. "브라우저를 새로고침해도 진행 중인 답변을 이어받을 수 있어야 한다"는 요구를 해결한다. 흘려보낸 청크를 로그로 남겨두어 재접속 시 놓친 구간을 재생(replay)할 수 있다.

### HITL (Human-in-the-Loop)

위험한 도구 호출이나 확인이 필요한 시점에 실행을 일시정지하고 사람의 승인·응답을 기다리는 메커니즘이다. 정지 시 그래프 상태를 체크포인트에 저장한다. 승인은 몇 분~몇 시간 뒤에 이뤄질 수 있으므로 인메모리가 아닌 영속 저장이 필요하다.

---

## 6. 인프라 · 저장소

### MongoDB

JSON 형태의 문서를 저장하는 문서형(NoSQL) 데이터베이스다. 정형화된 테이블 대신 유연한 구조의 문서를 다루기 좋아, 설정·대화 기록·메타데이터 등 스키마가 자주 바뀌는 데이터에 자주 쓰인다.

### Redis

메모리 기반의 초고속 키-값 저장소다. 여러 서버 인스턴스 간에 상태·이벤트·캐시를 공유하는 용도로 자주 쓰인다. 다만 "메시지 큐에 넣고 워커가 꺼내 실행하는 구조"와 "상태를 공유하는 관리 계층"은 다른 용도이므로, 어떤 목적으로 쓰이는지 구분해서 봐야 한다.

### pgvector

PostgreSQL에서 벡터(임베딩)를 저장하고 유사도 검색을 할 수 있게 해주는 확장이다. RAG처럼 "의미가 비슷한 문서 찾기"가 필요한 기능에서 자주 쓰인다.

---

## 부록: 자주 하는 개념 오해

| 오해 | 실제 |
|------|------|
| 질문하면 코드 인터프리터가 실행 흐름을 실시간 생성한다 | 실행 흐름을 만드는 주체와 코드 인터프리터는 별개다. 코드 인터프리터는 흐름이 호출하는 도구일 뿐이다 |
| LLM이 소스 코드를 써내고 그걸 실행한다 | 대개 설정 객체 조립 방식이며 동적 코드 실행(eval)은 쓰지 않는 경우가 많다 |
| 그래프는 서버 시작 때 한 번 만들어 재사용한다 | 요청 1건마다 새로 조립·컴파일하는 구조일 수 있다 |
| Agent별로 프로세스·컨테이너가 생성된다 | 반드시 그렇지 않다. 별도 샌드박스는 코드 실행 같은 위험한 도구에만 쓰이는 경우가 많다 |
| 메모리·캐시 저장소는 곧 실행 워커 큐다 | 상태·이벤트 공유용 관리 계층과 실행 큐는 다른 용도다 |
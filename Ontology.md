# 온톨로지 (Ontology)

> 어떤 분야의 **개념(무엇이 있는가)과 그 개념들 사이의 관계(어떻게 연결되는가)를 명시적으로 정의**한 지식의 구조.
> 한 줄 요약: **"개념들의 지도 + 그것들이 서로 맺는 관계의 규칙."**

> 관련 노트: 키워드 검색 · RAG · (다음 예정) GraphRAG.
> 이 문서는 검색 기법이 아니라 **지식을 어떻게 표현·구조화하는가(지식 표현)** 를 다루며, GraphRAG의 밑바탕이 된다.

---

## 1. 온톨로지란?

온톨로지는 원래 철학 용어("존재론")지만, IT에서는 **"어떤 영역의 지식을 컴퓨터가 다룰 수 있게 개념·관계로 정리한 것"** 을 뜻한다.

핵심은 두 가지를 명시하는 것이다.

- **어떤 개념(엔티티)이 존재하는가**: 사람, 의사, 병원, 질병 …
- **그 개념들이 어떻게 연결되는가**: 의사는 사람이다, 의사는 병원에서 일한다, 병원은 조직이다 …

> 비유: **사전**은 단어의 뜻만 알려주지만, **온톨로지는 개념들이 서로 어떻게 관계 맺는지까지** 그린 지도다.
> "의사"의 뜻뿐 아니라, 의사–사람–병원–질병이 어떤 관계로 얽히는지를 구조로 담는다.

---

## 2. 왜 중요한가 / 무엇에 쓰나

- **의미를 구조화**: 흩어진 정보를 "개념과 관계"로 정리해 기계가 이해·추론하게 한다.
- **추론(inference)**: 명시된 관계로부터 새 사실을 끌어낸다. (예: "의사는 사람" + "사람은 이름이 있다" → "의사는 이름이 있다")
- **일관성·공유**: 같은 용어를 조직·시스템이 같은 의미로 쓰게 한다 (용어 통일).
- **활용처**: 검색 강화, 추천, 의료·법률·금융의 지식 시스템, 그리고 **지식 그래프 / GraphRAG**.

---

## 3. 핵심 구성요소

| 요소 | 뜻 | 예 |
|------|-----|-----|
| **클래스(Class)** | 개념의 종류(범주) | 사람, 의사, 병원, 질병 |
| **인스턴스(Instance)** | 클래스의 실제 사례 | "김철수"(의사), "서울병원"(병원) |
| **속성(Property)** | 개체가 가진 특성 | 이름, 나이, 전문분야 |
| **관계(Relation)** | 개체 간 연결 | 근무한다, 진료한다, 소속이다 |
| **계층(Taxonomy)** | 상하위 분류 (is-a) | 의사 **is-a** 사람 |
| **규칙/공리(Axiom)** | 반드시 성립하는 제약 | "한 사람은 한 번에 한 곳에만 소속" 등 |

> 가장 중요한 뼈대는 **클래스(개념) + 관계 + 계층(is-a)** 이다. 여기에 규칙을 더하면 추론이 가능해진다.

---

## 4. 헷갈리기 쉬운 개념 구분

온톨로지 주변에는 비슷한 용어가 많다. 층위로 정리하면 명확하다.

| 용어 | 무엇인가 | 온톨로지와의 관계 |
|------|----------|-------------------|
| **택소노미(Taxonomy)** | 상하위 분류 체계(is-a 트리)만 | 온톨로지의 **부분집합**(계층만 있는 단순형) |
| **온톨로지(Ontology)** | 개념 + 다양한 관계 + 규칙 | 택소노미보다 풍부 (관계·추론 포함) |
| **지식 그래프(Knowledge Graph)** | 온톨로지 구조에 **실제 데이터**를 채워 그래프로 만든 것 | 온톨로지=설계도, 지식 그래프=그 도면대로 채운 실제 건물 |
| **DB 스키마** | 데이터 저장 구조 정의 | 비슷하나, 온톨로지가 더 **의미·관계·추론** 중심 |

> 한 문장 정리: **택소노미(분류) ⊂ 온톨로지(분류+관계+규칙), 그 온톨로지에 데이터를 채우면 지식 그래프.**

### 온톨로지 · 지식 그래프 · GraphRAG — 집짓기 비유

이 셋은 "같은 재료를 다른 단계에서 부르는 이름"이라 헷갈리기 쉽다. 집짓기로 보면 명확하다.

> **온톨로지 = 설계도, 지식 그래프 = 그 설계도대로 지은 건물, GraphRAG = 그 건물을 활용해 답을 찾는 서비스.**

| 단계 | 무엇인가 | 비유 | 실제 데이터? | AI 관련? |
|------|----------|------|--------------|----------|
| **온톨로지** | 개념·관계의 **종류와 규칙(틀)** | 설계도 | ❌ (틀만) | ❌ (표현 개념) |
| **지식 그래프** | 그 틀에 채운 **실제 데이터** | 지어진 건물 | ✅ | ❌ (저장 구조) |
| **GraphRAG** | 그 그래프로 검색해 **LLM이 답** | 건물을 쓰는 서비스 | ✅ 활용 | ✅ (RAG 기법) |

- **온톨로지**: "감독·영화·배우가 있고, 감독은 영화를 감독한다"는 **규칙만**. 아직 "봉준호"는 없다.
- **지식 그래프**: 그 틀에 실제 데이터를 채움 → `(봉준호)-감독-(기생충)-출연-(송강호)`.
- **GraphRAG**: 이 그래프를 창고로 삼아, 질문의 개체를 찾고 관계를 따라 모은 정보를 LLM에 근거로 넘겨 답한다.

> 셋이 꼭 다 필요하진 않다. **온톨로지 없이** LLM이 문서에서 개체·관계를 자유 추출해 지식 그래프를 만들 수도 있다(구축 쉬움·일관성↓).
> 즉 **온톨로지는 (선택적) 밑바탕, 지식 그래프는 결과물, GraphRAG는 그 활용**이다.

---

## 5. 트리플과 지식 그래프 (GraphRAG로 가는 다리)

온톨로지로 표현된 지식은 흔히 **트리플(triple)** 이라는 최소 단위로 저장된다.

```
(주어, 관계, 목적어)  =  (Subject, Predicate, Object)

예:
(김철수, 근무한다, 서울병원)
(김철수, is-a,   의사)
(서울병원, is-a,  병원)
(의사,   is-a,   사람)
```

이 트리플들을 연결하면 **그래프(노드 = 개체, 엣지 = 관계)** 가 된다.

```
[김철수] ──근무한다──▶ [서울병원]
   │ is-a                 │ is-a
   ▼                      ▼
 [의사] ──is-a──▶ [사람]  [병원] ──is-a──▶ [조직]
```

- 이렇게 개체와 관계를 그래프로 표현한 것이 **지식 그래프(Knowledge Graph)** 다.
- **GraphRAG는 바로 이 그래프를 검색에 활용**한다. 문서를 벡터로만 보는 게 아니라, "누가–무엇을–어떻게" 관계를 따라가며 답을 찾는다. (다음 노트에서 상세)

---

## 6. 표현 방식·기술 (개념만)

온톨로지를 실제로 기술하는 표준들이 있다. 깊이 몰라도 되지만 이름은 알아두면 좋다.

| 기술 | 역할 |
|------|------|
| **RDF** | 지식을 트리플(주어-관계-목적어)로 표현하는 기본 모델 |
| **RDFS / OWL** | 클래스·계층·규칙 등 더 풍부한 의미와 추론을 정의 |
| **SPARQL** | 지식 그래프(RDF)에 질의하는 언어 (SQL의 그래프 버전 느낌) |

> 실무에서 지식 그래프 DB(예: Neo4j 등)를 쓰기도 한다. 이들은 트리플/그래프를 저장·탐색하는 데 최적화돼 있다.

---

## 7. 구체 예시 — 영화 도메인

작은 온톨로지를 그려보면 감이 온다.

- **클래스**: 영화, 인물, 장르
- **관계**: 감독한다, 출연한다, 장르이다
- **트리플 예**:
  - (기생충, 감독, 봉준호)
  - (기생충, 장르, 드라마)
  - (봉준호, is-a, 인물)
  - (송강호, 출연, 기생충)

이 구조가 있으면 "봉준호가 감독한 드라마 영화에 출연한 배우"처럼 **관계를 따라가는 질의**가 가능해진다. 키워드 검색으론 어려운, 연결 기반 탐색이다.

---

## 8. 장점과 한계

### 장점
- **관계 기반 추론**이 가능 (연결을 따라 새 사실 도출).
- 의미·용어의 **일관성·공유**.
- "연결"을 묻는 복잡한 질의에 강함 (누가–무엇을–통해–무엇과).

### 한계
- **구축 비용이 크다**: 개념·관계·규칙을 사람이 설계·정의해야 함.
- **유지보수 부담**: 지식이 바뀌면 온톨로지도 갱신해야.
- **커버리지 한계**: 정의되지 않은 관계는 표현·추론 불가.
- 비정형 텍스트를 그대로 다루는 데는 벡터 검색이 더 편할 때가 많음.

---

## 9. AI · LLM과의 연결

- **지식 그래프**: 온톨로지에 데이터를 채운 그래프. LLM에 **구조화된 사실**을 공급하는 근거가 된다.
- **GraphRAG**: 벡터 검색 대신(또는 함께) 지식 그래프를 활용하는 RAG. 관계·다단계 연결이 중요한 질문에서 일반 RAG보다 유리할 수 있다. → 다음 노트
- **환각 억제**: 명시적 사실 그래프에 근거하면, 애매한 텍스트 검색보다 정확한 근거를 댈 수 있다.

---

## 10. 참고자료

- RDF / OWL / SPARQL (W3C 표준) 개념 자료
- 지식 그래프 및 그래프 DB(Neo4j 등) 문서
- 관련 노트: 키워드 검색, RAG, GraphRAG(예정)

---
